{
  "name": "flightpath-ui",
  "description": "Ground Control Station for Flightpath",
  "version": "0.0.1",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/flightpath-dev/flightpath-ui"
  },
  "scripts": {
    "build": "turbo run build",
    "check-types": "turbo run check-types",
    "clean": "turbo run clean",
    "dev": "turbo run dev",
    "fix": "turbo run fix",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "prettier": "^3.7.4",
    "rimraf": "^6.1.2",
    "turbo": "^2.7.4"
  },
  "packageManager": "pnpm@10.28.0",
  "engines": {
    "node": ">=22"
  }
}
